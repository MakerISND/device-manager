rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow public read/write since the app now uses a custom password check.
      allow read, write: if true;
    }
  }
}
